# API defaults (also set in Kubernetes deployment)
PORT=4000
DB_HOST=db
DB_PORT=5432
DB_NAME=study
DB_USER=study
DB_PASSWORD=study

# Web default (used by Next.js server route handlers)
API_BASE_URL=http://api
